import { Component } from '@/components/global/component';
import { Layout } from '@/components/global/layout';
import { NotFound } from '@/components/not-found';

const FourHundredFour: Component = () => {
  return (
    <Layout>
      <NotFound />
    </Layout>
  );
};

export default FourHundredFour;
